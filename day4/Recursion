#include <bits/stdc++.h>
using namespace std;
void print_name(int i, int n)
{
    if (i > n)
        return;
    cout << n;
    print_name(i, n - 1);
}
int main()
{
    int n;
    cin >> n;
    print_name(1, n);
    return 0;
}
