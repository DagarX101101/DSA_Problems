#include <bits/stdc++.h>
using namespace std;
#define vi vector<int>
#define inputv(v)     \
    for (auto &i : v) \
        cin >> i;
#define all(v) v.begin() v.end()
#define int long long
int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n;
    vi v;
    inputv(v);
    solve(v) return 0;
}
